# Ubiquitous language

Canonical Cell One words. Locked defaults stay in [`../charter.md`](../charter.md). `estate help north-star` and `estate help charter` print the product sentence. Prepare: [`TRAIN-ENRICH.md`](TRAIN-ENRICH.md). Walks: [`operator-enrich-journeys.md`](operator-enrich-journeys.md).

## Terms

### estate

Desired-state file. `kind: agent-estate`. It names lanes, agents, intentions, model bindings, sacred exclusions, enrich packs, and placements. `examples/estate.yaml` is hash-locked. The estate file is the source of truth for desired state. Code source of truth is [github.com/jwgale/agent-estate-factory](https://github.com/jwgale/agent-estate-factory). Fail-closed check is the Rust validator. JSON Schema files are documentary.

### lane

Horizon, Research, and Sanctum. Separate. Own-lane memory read is allowed. Cross-lane memory is deny-default. Sanctum is the lane `sanctum`. The exclusion id is `cyera-ci`. Rust classroom stays an exclusion.

### plan

Human control surface. `estate plan` writes reviewable markdown under `plans/`. Blast radius a person can read before apply. `apply --require-plan` and `--require-fresh-plan` read those files.

### apply

Converge desired state. Binds box sessions. Records placement leases. Writes regenerable actual state, the desired snapshot, model actual, and placement actual. Audited. `--dry-run` writes nothing.

### lease

Durable grant. Optional TTL. Placement leases: `.cell/placement-actual.json`. Hop leases: `.cell/conveyor-leases.json`. Expired leases refuse. `estate expire --forget` drops expired placement rows so apply can restamp. Convey forget keeps hop declarations.

### pack

Enrich pack. Curator edit instructions. `policy: manual`. `estate packs propose` writes a proposal and leaves it unapplied. `estate packs accept --curator jason` writes the instruction file. Jason pastes into the estate file. `source_drivers` are `frontier` and/or `local`. Promote stays refused.

### curator

Jason. Locked `enrich_packs.curator: jason`. A wrong curator is `refuse:curator`.

### enrich

Curator work on a pack: edit instructions Jason pastes into the estate. `policy: manual`. `estate enrich prepare` writes artifacts for a purpose-built SLM (`--all-drivers` writes every card the job allows). `estate enrich from-pack` runs that prepare for an accepted pack into `.cell/enrich`. `estate enrich list` reads `.cell/enrich`. `estate enrich import-prepared` writes a `local_slm` binding proposal and does not apply. `estate enrich apply-proposal` stages that proposal under `.cell/enrich-stage/`. `estate apply --require-plan` writes the source estate. The job field is `train` or `enrich`. The default job is `enrich`. `llamafactory-lora`, `llamafactory-qlora`, `axolotl-lora`, and `axolotl-qlora` default to `train`. Prepare does not train, does not POST, and does not rewrite the estate. Modelfile `FROM` is the seated model: `params.model` on the local binding, or a pack `model_hint` that is already a model tag. The binding id `local_slm` is not that tag. A missing seated name is `refuse:base-model`. The train base for `llamafactory-lora`, `llamafactory-qlora`, `axolotl-lora`, and `axolotl-qlora` is a separate field (`train_base_model` on the pack, or `params.train_base_model` on the local binding): a Hugging Face repo id or a local directory of HF weights. A bare Ollama seat tag in that field is `refuse:train-base`. `dataset.jsonl` is a scaffold unless `--from-feed` copies rows already under the cell state directory. A missing file with that flag is `refuse:dataset`. Command surface: [`TRAIN-ENRICH.md`](TRAIN-ENRICH.md). Walks: [`operator-enrich-journeys.md`](operator-enrich-journeys.md). Live handoff: [`LIVE-PROBES.md`](LIVE-PROBES.md).

### purpose-built SLM

A small-parameter model crafted for one job. Open-source SLMs will get more common. The suite facilitates training and enrichment of these models. The outcome belongs to the estate.

### TrainEnrichDriver

Data-plane trait in `model-estate`. Methods: `id()`, `prepare(job)`, catalog `status` and `probe`. Cards on `main`: `ollama-modelfile` (integrate Ollama `create` / Modelfile `FROM` + `SYSTEM`; `FROM` is the seated model, never the binding id), `external-manifest` (portable JSON/YAML for a later trainer), `llamafactory-lora` (a LLaMA-Factory LoRA recipe with no quantization; `model_name_or_path` is the train base, separate from the Ollama seat tag; rank 8; default job `train`; does not require bitsandbytes; the factory does not run LLaMA-Factory; after train, `merge-adapt` prints `llamafactory-cli export`), `llamafactory-qlora` (a LLaMA-Factory QLoRA recipe; `model_name_or_path` is that same train base; default job `train`; the factory does not run LLaMA-Factory; `merge-adapt` prints that same export), `axolotl-lora` (bf16 Axolotl LoRA matching `examples/llama-3/lora-1b.yml`; `base_model` is that same train base, separate from the Ollama seat tag; default job `train`; the factory does not run Axolotl; after train, `estate enrich merge-adapt` prints `axolotl merge-lora`), and `axolotl-qlora` (4-bit Axolotl QLoRA matching `examples/llama-3/qlora.yml`; that same train base; default job `train`; the factory does not run Axolotl; `merge-adapt` also prints `--dequant`). `unsloth-qlora` is an optional NEXT card (`status` `optional`): an Nvidia-only QLoRA handoff that writes `UNSLOTH.md` and does not write a script or call Unsloth. After that train, `merge-adapt` prints `save_pretrained_merged` with `save_method` `merged_16bit`. `mlx-lm-lora` is an optional NEXT card (`status` `optional`): an Apple Silicon LoRA handoff that writes `MLX.md` when `host_class_affinity` is `apple-silicon` and does not write a script or call mlx-lm. Another affinity is `refuse:host`. After train, `merge-adapt` prints `mlx_lm.fuse` and `local-seat` prints the GGUF from `--export-gguf`. A later train/enrich entrant is another card. llama.cpp INI presets stay off this catalog until a pack or binding names a GGUF path. Floor and control dispatch do not match driver ids.

### train/enrich facilitation

The factory makes room for training and enrichment of purpose-built SLMs. The durable beachhead is `TrainEnrichDriver`, curator packs, and the specialist path (`estate specialist`, `local_slm`). Prepare writes artifacts. `llamafactory-lora` writes the unquantized LLaMA-Factory LoRA recipe an operator runs outside the factory, `llamafactory-qlora` writes the QLoRA recipe, `axolotl-lora` writes the bf16 Axolotl YAML, `axolotl-qlora` writes the 4-bit Axolotl YAML, and `import-trained` hands the adapter back through the existing proposal. Target C is the popular Qwen QLoRA ladder: that `llamafactory-qlora` prepare, the external train and export lines, a printed GGUF convert, a printed Ollama create, and `import-trained` to record the shape. Target A is the Qwen LoRA ladder: that `llamafactory-lora` prepare, the external train and export lines, a printed merge, a printed GGUF convert, a printed Ollama create, and `import-trained` to record the shape. `make seat-journey` prints the Target C merge, convert, seat, and import lines against fixture stubs. After the GGUF `local-seat` print, the standing next step is `import-trained` for that GGUF. The proposal stays `auto_apply=false`. The script does not run `ollama create`, does not apply the estate, and does not convert. `--from-feed` copies instruct rows already on disk. List reads them. Import-prepared proposes the `local_slm` join and leaves plan/apply to Jason. A dataset downloader and an in-process trainer stay unshipped. No new crate. Command surface: [`TRAIN-ENRICH.md`](TRAIN-ENRICH.md). Walks: [`operator-enrich-journeys.md`](operator-enrich-journeys.md).

### frontier

Equal-class model binding. Cell One id `xai_grok`. Driver `frontier-http`. Live model `grok-4.7` when `XAI_API_KEY` is set. The binding model is `params.model` on the estate. The catalog schema card prints `grok-4.7`. `CELL_FRONTIER_MODEL` names the live POST. Three fields. A cell catalog that disagrees with the binding is `refuse:frontier-model`.

### local

Equal-class model binding. Cell One id `local_slm`. Class `local`. The binding is the estate contract. The process behind it sits in the local-runtime seat.

### local runtime (driver)

A local runtime is an ecosystem seat, held by a driver. Software on a host runs a model. Catalog, route, and bind take any entrant. Endpoint: `CELL_LOCAL_ENDPOINT`.

Ollama fills the seat today. Another process can fill it tomorrow. llama.cpp is the swap-proof entrant on the same specialist protocol. Ollama is local-run software: it lets a user run models. The estate is the product. The binding id stays `local_slm`.

Supported means the seated driver is green on the box. Today that is `ollama` and llama.cpp. vLLM and TRT stay experimental catalog cards. Native MLX stays a stub. Apple path: Ollama-on-Mac uses the `ollama` driver in that seat.

Local down audits `model.local.down`. Estate-bound local work stops.

### entrant

A process that can fill the local-runtime seat. `ollama` is the entrant on `main`. llama.cpp enters on the same specialist protocol. A later process enters through catalog / route / bind. A train/enrich entrant is separate: another `TrainEnrichDriver` card. It does not replace the seat.

### integrate-vs-invent

Build rule. A feature earns its keep. If `ollama` or llama.cpp already does the job, tighten that driver. Ollama already creates a model from a Modelfile. `ollama-modelfile` writes that file. A from-scratch local server, and a from-scratch trainer, wait until the entrant does not already do the job.

### sacred

Dual-layer exclusions. Hardcoded `cyera-ci` and `rust-classroom` always apply. `policy/sacred.yaml` overlays add names (`lab-notebook` in fixtures). A file cannot drop a locked id. An allow intention cannot punch through.

### refuse

Fail-closed stop. CLI and drift use the `refuse:` prefix. Examples: `refuse:help-topic`, `refuse:curator`, `refuse:frontier-model`, `refuse:frontier-invent`, `refuse:sacred-id`, `refuse:cloud-spawned`, `model.local.down`. Unknown actions, hardware SKUs in binding or probe ids, and sacred names stop before the write or the POST.

### placement

Where agents run. `box` is this Cell One. `cloud-agent` (`cursor-cloud`) is a declared lease. Floor records the lease. Spawn of that cloud lease stays parked. `host_class` is `consumer-nvidia` | `apple-silicon` | `rented-nvidia` | `any`. Hardware is a driver choice.

### convey

`estate convey` is a lease-bound hop stub. Verbs: hop, call, list, leases, sync, expire. Files: `.cell/conveyor-mesh.json`, `conveyor-hops.json`, `conveyor-leases.json`. Call requires a granted lease. A spawned cloud hop lease refuses before JSON, restamp, or forget. Hop transport stays parked. Hop uses `--intention-kind` (distinct from hop `--kind`). An allow hop still refuses when the declared capability does not match the placement-derived coverage capability.

An `agent` intention is who may call whom. Kind `agent` (estate YAML and CLI aliases `agent_call`, `agent-call`). The object is an estate agent id, bare or `agent:`. The subject lists that id under `calls`. Missing coverage is deny-default. An explicit deny wins. Sacred ids are not call targets. When that id is also a tool, MCP, mount, or model on the same agent, pass `--kind agent` or `--intention-kind agent`, or use the `agent:` prefix. `lane-tool` and `mesh-stub` stay hop capabilities and are not inferred as Agent. Plan, drift, and doctor cite each edge as `own` (the same agent) or `peer` (another agent). `estate apply` and dry-run cite a declared call in that same line before any write. Allow is quiet. Deny and deny-default are notes and do not fail apply. A call target that is not an estate agent fails before writes. Agent-call rows have no capability-mismatch class. Undeclared pairs stay on plan, drift, and doctor.

`estate convey hop --agent` names the population on the hop lease. Empty agents are not a grant. `estate convey hop` and `estate convey sync` refuse a placement hop whose capability disagrees with placement-derived coverage (`lane-tool` on box, `mesh-stub` on cloud) and do not write the mesh. A hop id that is not a placement stays the lease stub. `estate convey sync` copies `agents` from `placement-actual.json` onto the matching hop. A lease that lists an agent the placement row does not is `refuse:agent-unplaced` and writes nothing. `estate convey call --agent <id>` refuses when that agent is not on the lease (`refuse:agent-unbound`). When the agent is on the lease, the call checks the estate intention for the capability (`refuse:intention` on deny). That check runs only if something invokes the call. `estate convey authority` is a file check: `would-allow`, `would-deny`, or `not-enforced`. It prints the same Agents section as plan, drift, apply, status, and doctor (`describe_agents_section`), then the same hop coverage cites doctor and status print from `hop_coverage_cites` (`FAIL` on a capability mismatch, `note` on deny and deny-default), then that Authority section. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list. Deny and deny-default on the Agents text and on those cites stay notes and do not fail the command. A missing or unreadable estate refuses before any section. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or a placement-actual parse failure refuses before any section and does not invent cites, Agents, or Authority rows. The shared stack still reads placement-actual for mesh interpretation and Authority. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and this file check succeeds. There is no later reader. That diverges from `estate convey list`, `estate convey leases`, `estate convey expire`, `estate convey sync`, `estate convey hop`, and `estate convey call`, whose readers still refuse that SKU after the stack. `conveyor-proxy authority` does not call `honesty_stack` or `render_hop_coverage_cites` (those live in estate-control). The proxy still refuses a mesh that does not parse, a bad host_class on that file, `refuse:agent-unplaced`, and a placement-actual parse failure before Agents. A placement-actual SKU omits Authority and the proxy command succeeds, without hop cites. The command does not spawn. A granted box lease whose hop coverage is deny, deny-default, or a capability mismatch is `would-deny` and cites that hop coverage. A match continues to the estate intention. A hop id that is not a placement stays the lease stub. It does not write, and it does not show that a worker called the conveyor. `estate plan` prints an Authority section after the Agents / Security-as-IaC blast and the coverage cites, including hop refuse. The section is `authority_report`, the same rows as `estate convey authority`, via `describe_authority_section`: summary counts `would-allow`, `would-deny`, and `not-enforced`, and one line per row. A missing `conveyor-mesh.json` leaves those rows `not-enforced`, cites that the mesh file is absent, and does not invent a lease. A present mesh whose leases omit a declared capability stays `not-enforced` and says no hop lease names that capability. The same section prints `not-enforced reasons:` after the counts when that count is non-zero, with zero classes omitted: `missing-mesh`, `no-lease`, `cloud`, `ungranted`, `empty-population`, and `other`. A cloud declared-not-spawned row stays `cloud`. `would-allow` and `would-deny` stay on the count line. The section does not write, and it does not show that a worker called the conveyor. It has no `enforced` status. Plan still bails on a hard coverage cite or a hop-coverage mismatch before a reviewed copy. `estate drift` prints that same Authority section after its Agents section and coverage lines, including hop refuse, from `describe_authority_section` over `authority_report`. A missing mesh stays `not-enforced`. Drift still bails on floor or model drift and on a hop-coverage mismatch. The section does not write. `estate apply` and `estate apply` dry-run print that same Authority section after the Agents section and the coverage cites, including hop refuse, from `describe_authority_section` over `authority_report`, and before any estate, mesh, lease, or apply-audit write. A missing mesh stays `not-enforced`. A hop-coverage mismatch prints the section, then exits on `refuse:hop-coverage`, and does not write. Dry-run writes nothing. `estate doctor` and `estate doctor --strict` print the same Agents section as plan, drift, apply, status, and convey authority (`describe_agents_section`: id, lane, desktop, placement, declared counts, and allow / deny / deny-default coverage, including agent-call own and peer) after the hop describe lines and before hop coverage cites and that same Authority section from `describe_authority_section` over `authority_report`. Deny and deny-default stay notes and do not fail doctor, including `--strict` on the locked example. A missing mesh stays `not-enforced`. A hop-coverage mismatch still fails doctor after the Agents section and the Authority section are printed, and doctor does not write the mesh, the leases, the estate, or the apply audit. Doctor does not spawn. A mesh that does not parse does not invent Agents or Authority rows. `estate status` prints the same Agents section as plan, drift, apply, doctor, and convey authority (`describe_agents_section`: id, lane, desktop, placement, declared counts, and allow / deny / deny-default coverage, including agent-call own and peer) after the hop expired count and the cloud-agent line, then the same hop coverage cites doctor and drift print from `hop_coverage_cites` / `render_hop_coverage_cites` (`FAIL` on mismatch, `note` on deny and deny-default), then that same Authority section from `describe_authority_section` over `authority_report`, via `honesty_stack`. Deny and deny-default on the Agents section and on those cites are notes and do not fail status. A mismatch cite does not fail status. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. A mesh that does not parse, a bad host_class on that file, or `refuse:agent-unplaced` does not invent cites or Authority rows. The shared stack still reads placement-actual for mesh interpretation and Authority when the page reaches it. A placement-actual SKU host_class does not reach the stack. `refuse_lease_host_classes` refuses that SKU before the page. `list_expired_hop_leases` would also refuse it (`load_interpreted_mesh` slim-parses placement-actual) before the page, and does not run once the lease host-class check has returned. The page does not print. Authority rows are not omitted after a printed header, and the command does not succeed with Authority omitted. There is no later mesh reader after the stack. That diverges from `estate convey authority`, from `estate leases` and `estate convey sync`, and from `estate audits` and `estate history`. Status does not spawn. The section does not write the mesh, the leases, or the estate. `estate reconcile` and `estate reconcile --suggest` print that same Agents section, then those hop coverage cites (`FAIL` on a capability mismatch, `note` on deny and deny-default), then that Authority section, after the estate loads and before `reconcile.json`, a suggest patch, or the placement report. Those cites do not fail reconcile. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`). A mesh that does not parse, or a bad host_class on that file, refuses before those sections and does not invent cites, Agents, or Authority rows. A placement-actual SKU host_class still reaches the placement report. A mesh population ahead of placement-actual is `refuse:agent-unplaced` before those sections and before `reconcile.json` or a suggest patch. Placement drift still fails closed. `--suggest` does not rewrite leases. Reconcile does not spawn and does not rewrite the estate or the mesh. `estate audit export` writes that same Agents section, then those hop coverage cites, then that Authority section into `honesty.md` next to `MANIFEST.md` after the estate loads and before the copy finishes. The manifest names `honesty.md`. Those cites do not fail the export. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`). The bundle copies `conveyor-mesh.json` when that file is present. A mesh that does not parse, or a bad host_class on that file, refuses before `honesty.md` and before the out dir is wiped. A placement-actual SKU host_class still exports; `honesty.md` keeps Agents and hop cites and omits Authority rows. A mesh population ahead of placement-actual is `refuse:agent-unplaced` before `honesty.md` and before the out dir is written. The export does not spawn and does not rewrite leases, the estate, the apply audit, or the live mesh beyond the reconcile files it already writes. `estate leases` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before the placement lease list. Those cites do not fail leases. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`). A mesh that does not parse, or a bad host_class on that file, refuses before those sections and before the lease list. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and the lease reader still refuses that SKU before the placement JSON. A mesh population ahead of placement-actual is `refuse:agent-unplaced` before those sections and before the lease list. Leases does not spawn and does not write the mesh, the leases, the estate, or the apply audit. `estate convey leases` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before the hop lease list. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`). A mesh that does not parse, or a bad host_class on that file, refuses before those sections and before the hop lease JSON. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and the hop lease reader still refuses that SKU before the hop lease JSON. A mesh population ahead of placement-actual is `refuse:agent-unplaced` before those sections and before the hop lease JSON. A spawned cloud hop still refuses before the JSON. The command does not spawn and does not write the mesh, the leases, the estate, or the apply audit. `estate audits` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before the apply-audit list. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`). The shared stack still reads placement-actual for mesh interpretation and Authority. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or any other mesh error including a placement-actual parse failure, refuses before those sections and before the apply-audit list. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, then the apply-audit list still prints. There is no second placement refuse before that list. The command does not spawn and does not write the mesh, the leases, the estate, or the apply audit. `estate history` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before the lifecycle history list. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`). The shared stack still reads placement-actual for mesh interpretation and Authority. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or any other mesh error including a placement-actual parse failure, refuses before those sections and before the lifecycle history list. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, then the history body still prints. There is no second placement refuse before that list. The command does not spawn and does not write the mesh, the leases, the estate, or the apply audit. `estate expire` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before the expired placement lease list. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. The shared stack still reads placement-actual for mesh interpretation and Authority. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or any other mesh error including a placement-actual parse failure, refuses before those sections, before the expired list, and before `--forget` writes. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, then the expired placement list still prints. There is no second placement refuse before that list, same as `estate audits` and `estate history`. `list_expired_leases` and `forget_expired_leases` load placement-actual with `load_placements` and do not call `load_interpreted_mesh`. `--forget` drops expired placement rows after that list prints. An empty expired list does not rewrite. An expired spawned cloud-agent lease still refuses before the list and before the rewrite. Without `--forget` the command does not write the mesh, the leases, the estate, or the apply audit. A missing or unreadable estate refuses before those sections and before the list. The command does not spawn. `estate convey list` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before the hop decl list. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. The shared stack still reads placement-actual for mesh interpretation and Authority. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or any other mesh error including a placement-actual parse failure, refuses before those sections and before the hop list. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and the hop list reader still refuses that SKU before the hop JSON (`list_hops` loads the interpreted mesh). That diverges from `estate audits` and `estate history`, whose readers still print the body. The command does not spawn and does not write the mesh, the leases, the estate, or the apply audit. `estate convey expire` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before the expired hop lease list. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. The shared stack still reads placement-actual for mesh interpretation and Authority. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or any other mesh error including a placement-actual parse failure, refuses before those sections, before the expired list, and before `--forget` writes. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and the expired-lease reader still refuses that SKU before the expired list (`list_expired_hop_leases` loads the interpreted mesh) and before `--forget` rewrites the mesh. That diverges from `estate audits` and `estate history`, whose readers still print the body. `--forget` rewrites hop leases only after that list prints. An empty expired list does not rewrite. An expired spawned cloud hop still refuses before the list and before the rewrite. Without `--forget` the command does not write the mesh, the leases, the estate, or the apply audit. The command does not spawn. `estate convey sync` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before it writes the mesh, when `--estate` is a file. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. The shared stack still reads placement-actual for mesh interpretation and Authority. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or any other mesh error including a placement-actual parse failure, refuses before those sections and before the mesh write. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and sync still refuses that SKU before the mesh JSON and before the write (`sync_from_placements_covering` slim-parses placement-actual before `persist_mesh`). That diverges from `estate audits` and `estate history`, whose readers still print the body. A placement hop whose stamped capability disagrees with placement-derived coverage is `refuse:hop-coverage` after the stack and writes nothing. A spawned cloud placement still refuses after the stack and writes nothing. A missing estate file keeps the lease stub sync and does not invent this stack. On success the mesh JSON prints after the stack. The command does not spawn. `estate convey hop` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before `declare_hop_covering` writes the mesh and before the lease JSON. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. The shared stack still reads placement-actual for mesh interpretation and Authority. Intention deny, hop-coverage deny, deny-default, and a capability mismatch, and `--intention-kind` without `--agent` (`refuse:agent-unbound`), refuse before those sections and before the write. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or a placement-actual parse failure, refuses before those sections and before the write. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and `declare_hop_covering` still refuses that SKU before the lease JSON and before the write (`slim_parse_placement_actual` before `persist_mesh`). That diverges from `estate audits` and `estate history`, whose readers still print the body. An estate cloud-agent placement stays `refuse:hop-coverage` (deny) before the stack. A hop id that is not an estate placement stays the lease stub. When placement-actual marks that id spawned cloud, declare refuses `refuse:cloud-spawned` after the stack and writes nothing. `refuse_hop` (sacred id, a sku-banned id, a bad host_class on the hop decl, or an unknown kind) runs after the stack and before the write. A new hop whose agents are ahead of the placement row is `refuse:agent-unplaced` after the stack and writes nothing. The capability mismatch inside `declare_hop_covering` is the same gate the coverage refuse already applied, so this command does not reach a second mismatch after the stack. On success the lease JSON prints after the stack. The command does not spawn. `estate convey call` prints that same Agents section, then those hop coverage cites, then that Authority section, after the estate loads and before `call_hop` or `call_hop_for_agent` and before the call JSON. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. The shared stack still reads placement-actual for mesh interpretation and Authority. Intention deny, hop-coverage deny, deny-default, and a capability mismatch, a missing estate, and `--kind` without `--agent` (`refuse:agent-unbound`), refuse before those sections. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or a placement-actual parse failure, refuses before those sections and before the call. A placement-actual SKU host_class still continues: Agents and hop cites print and Authority rows are omitted, and `call_hop` / `call_hop_for_agent` still refuse that SKU before an allow and before any restamp (`load_interpreted_mesh` slim-parses placement-actual before `restamp_hop_from_decl`). That diverges from `estate audits` and `estate history`, whose readers still print the body. An estate cloud-agent placement stays `refuse:hop-coverage` (deny) before the stack. A hop id that is not an estate placement stays the lease stub. A missing lease is `refuse:no-lease` after the stack and does not invent a mesh. A present hop decl with no lease restamps once after the stack. A populated lease with no `--agent`, or an agent the lease does not name, is `refuse:agent-unbound` after the stack and does not print the call JSON. This command does not add a second mesh write. On success the call JSON prints after the stack. The command does not spawn. Whether a hop lease is the right shape for authority next to the workload is still uncertain. It is not an identity lookup. The identity equation stays parked. Not a gateway.

### decision receipt

`estate convey call` appends one versioned receipt at `{state-dir}/decisions/receipts.jsonl` after coverage and intention gates and before the allow or restamp JSON. Eligible candidates are opaque model-binding ids. The selector chooses one id or abstains. The host re-validates that choice as `ok`, `stale`, `ineligible`, or `expired` and may record a fallback. The selector does not grant permission. A placement-actual SKU host_class, a mesh that does not parse, and `refuse:agent-unplaced` write no receipt. Success prints one `decision receipt:` cite. A journal write that fails after the hop has already committed prints `decision receipt: journal write failed after hop commit` and still prints the allow or restamp JSON. The hop exit stands. Binding digests cover id, class, driver, and a stable serialization of binding params, so param drift marks a selector hint stale. Unread `estate decisions export` does not fail the call. `estate decisions export` writes JSONL replay cases. `estate decisions report` counts stage, validation, and fallback. No promote. No auto-apply.

`estate authorize` runs the same `authorize` check as `conveyor-proxy check`, then appends that same receipt after the check resolves and before the allow or deny JSON. On that line `hop_id` is the intention kind, `capability` is the object, and `surface` is `authorize`. Eligible candidates stay opaque model-binding ids. The selector chooses one id or abstains. Zero eligible ids and two or more eligible ids abstain. The host re-validates. A fallback is recorded and is not a grant. Authorize still decides allow or deny. A bad `{state-dir}/decision-select.json` is `refuse:decision-select` before the check and before the receipt. Success prints one `decision receipt:` cite. A journal write that fails after authorize has committed prints `decision receipt: journal write failed after authorize commit` and still prints the allow or deny JSON. The authorize exit stands. This path has no hop lease, so lease expiry stays on `estate convey call`. Export and report read the same journal. No honesty stack. No promote. No auto-apply. Real-world host → select → receipt walk: [`NORTH-STAR.md`](NORTH-STAR.md).

`estate complete` is the thin host → select → authorize → data-plane complete → receipt path. The same host prepare / select / revalidate loop runs. Omit `--object` when the selector chooses one eligible id with validation `ok`. Name `--object` to complete a specific binding; the selector still does not grant. Authorize still decides allow or deny. On allow, complete runs through `complete_via_binding` on the chosen binding's local or frontier driver. Control does not invent the text. Missing `CELL_LOCAL_ENDPOINT` or `XAI_API_KEY` fail-closes. A resolved allow, deny, or fail-closed complete appends one receipt with `surface` `complete`, `hop_id` `model`, and `capability` the binding complete targeted. Equal-class abstain without `--object` is `refuse:decision-abstain` and writes that receipt. A bad hint is `refuse:decision-select` before the check and before the receipt. Success prints one `decision receipt:` cite, then the completion JSON. A journal write that fails after complete has committed prints `decision receipt: journal write failed after complete commit` and still prints the completion. `estate specialist` stays the unbound delegate and does not journal. No honesty stack. No hop lease. No promote. No auto-apply. This path does not invent a live PASS. `READY_FOR_LIVE_TEST` stays no.

`estate models` prints that same Agents section, then those hop coverage cites, then that Authority section, after bindings, readiness, and per-binding ping lines, from `honesty_stack`. Those cites do not fail the command. A match stays quiet. A missing mesh is an empty cite list and stays `not-enforced` (`missing-mesh`) and cites that `conveyor-mesh.json` is absent. The shared stack still reads placement-actual for mesh interpretation and Authority. A mesh that does not parse, a bad host_class on that file, a mesh population ahead of placement-actual (`refuse:agent-unplaced`), or any other mesh error including a placement-actual parse failure, refuses before those sections. The models body is already printed and is not rewritten. A placement-actual SKU host_class (`MeshError::BadHostClass` from slim-parse) continues: Agents and hop cites print and Authority rows are omitted. There is no later mesh reader after the stack. The command does not refuse after the stack. A missing or unreadable estate refuses before the models body. The command does not spawn and does not write the mesh, the leases, the estate, or the apply audit.

### control

`estate-control`. Validate, plan, apply, drift, compile intentions. Charter sentence: control does not complete. Completion runs on the data plane (`model-estate`). `estate specialist` is a thin unbound delegate into that plane. `estate complete` is the estate-bound delegate: host → select → authorize, then `complete_via_binding`, then a decision receipt.

## Suite

Next-generation harness and custom AI creator suite. Pillars, in order:

1. Agents. Spin up and run agents as first-class under security-as-IaC. Sacred isolation, plan/apply, fail closed.
2. Harness. Grok Bot-like look and feel. The UI may still be deferred.
3. On-spot specialty SLMs. Create, train, enrich, and seat purpose-built small models when the work needs them. One facet. A local-runtime seat in the estate flow. Ollama today. Another process tomorrow. Catalog, route, and bind take the entrant. Facilitate training and enrichment of purpose-built small-parameter models. Open-source SLMs will get more common.
4. Optional later. A central model brain that learns from the user. Parked. Not a commitment.

Beachhead on `main`: the seated drivers, enrich packs, the specialist path, and `TrainEnrichDriver`. `estate enrich prepare` writes artifacts. `llamafactory-lora` writes a LLaMA-Factory LoRA recipe without quantization, `llamafactory-qlora` writes the QLoRA recipe the same way, `axolotl-lora` writes the bf16 Axolotl YAML, and `axolotl-qlora` writes the 4-bit Axolotl YAML; the factory does not run the trainer. Ollama is the local-run seat. That portable seat and facilitation of purpose-built SLMs are one facet of the suite. Integrate the driver that already does the job. `READY_FOR_LIVE_TEST` stays no. Command surface: [`TRAIN-ENRICH.md`](TRAIN-ENRICH.md). Walks: [`operator-enrich-journeys.md`](operator-enrich-journeys.md).

## Aliases to avoid

Draft word on the left. Charter term on the right.

| Draft word | Charter term |
| --- | --- |
| gateway, AI gateway, MCP catalog | control does not complete; conveyor allow/deny |
| Ollama wrapper-as-product | local-runtime seat; integrate the driver |
| LM Studio-alone, weight browser, UI-only shell | local-runtime seat inside the estate; harness look and feel stays in the suite |
| thin Grok Bot clone (look without the estate), undirected agent sprawl, computer-use farm | agents under security-as-IaC; lanes Horizon / Research / Sanctum |
| frontier-proxy-only | equal-class frontier and local; `model.local.down` |
| local-studio-only | equal-class bindings; purpose-built SLM stays in the suite |
| capability mesh (as the product) | convey, lease-bound hop stub. Path `.cell/conveyor-mesh.json` stays a filename |
| model factory | Agent Estate Factory |
| SKU in a binding or probe id (`5090`, `4090`, `m3-max`) | `host_class` |
| cloud spawn | placement `cloud-agent`, declared lease |

Suite words, kept: distillation, training path, purpose-built SLM.

Host-class spellings that normalize: `rtx-consumer` / `rtx_consumer` → `consumer-nvidia`; `nvidia-rental` / `nvidia_rental` → `rented-nvidia`.

Sacred spellings that refuse: `cyera` / `cyera_ci` → `cyera-ci`; `rust_classroom` → `rust-classroom`.

## help

On `main`: status, plan, apply, reconcile, feed-loop (`feed`), backup, frontier, day90-mixed (`mixed`), north-star (`northstar`), charter, enrich (`train`). `north-star` and `charter` print one page. `enrich` and `train` print the prepare page. An unknown topic is `refuse:help-topic`. Prepare: [`TRAIN-ENRICH.md`](TRAIN-ENRICH.md). Walks: [`operator-enrich-journeys.md`](operator-enrich-journeys.md).

## Planes

Control, data, and feed stay separate. The boundary table is in [`../charter.md`](../charter.md). Control to data is apply. Data to control is drift. Data to feed is scrubbed events. Feed to control is an explicit pack import. Feed to data is empty in Cell One.
