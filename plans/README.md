# Plan history

`estate plan` appends `plan-<timestamp>-<hash>.md` and `.json`, then rewrites `INDEX.md`.

These generated files are gitignored so the repo does not thrash. When a blast-radius should be PR-reviewed, copy or `git add -f` that markdown into the PR.

`estate plans` lists what is on disk.
