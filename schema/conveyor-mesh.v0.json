{
  "schema": "cell-one.conveyor-mesh.v0",
  "note": "Capability mesh beachhead. Declare a hop, lease-bound call, refuse without a granted lease. Optional agents names the population; empty is not a grant. Call --agent refuses an unbound agent and an estate intention deny. A lease agent the placement row does not list is refuse:agent-unplaced. Optional hop ttl_secs stamps issued_at/expires_at. Call refuses expired. Cloud-mesh hops are declared, not spawned. Identity stays parked. Not a gateway.",
  "kinds": ["box", "cloud-mesh"],
  "durable": [".cell/conveyor-mesh.json", ".cell/conveyor-hops.json", ".cell/conveyor-leases.json"],
  "hops": [
    {
      "id": "cell-one-box",
      "kind": "box",
      "capability": "lane-tool",
      "host_class": "any",
      "wired": true
    },
    {
      "id": "cursor-cloud",
      "kind": "cloud-mesh",
      "capability": "mesh-stub",
      "host_class": "any",
      "wired": false
    }
  ],
  "leases": [
    {
      "hop_id": "cell-one-box",
      "kind": "box",
      "capability": "lane-tool",
      "host_class": "any",
      "granted": true,
      "spawned": true,
      "durable": true,
      "driver": "box"
    },
    {
      "hop_id": "cursor-cloud",
      "kind": "cloud-mesh",
      "capability": "mesh-stub",
      "host_class": "any",
      "granted": false,
      "spawned": false,
      "durable": true,
      "driver": "cloud-mesh"
    }
  ]
}
