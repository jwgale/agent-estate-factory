# Specialist enrich packs

Jason curates the first packs by hand. Policy is **manual**. Feed never auto-promotes a pack into the estate.

Live drop zone: [`packs/`](../../packs/). Documentary copies stay here. `estate feed pack` writes candidate `{id}.pack.json` from `.cell/feed/events.jsonl`. `estate feed import` is explicit apply and does not rewrite the estate. Cell One ships `packs: []` on the estate until Jason adds an id by hand. Not a model library or studio.

Packs are specialist jobs (policy-precheck, redact, later curated skills). They are not a weight browser.
